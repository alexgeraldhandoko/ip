package dippy;

import javafx.application.Application;

/**
 * Launcher is the entry point to start the UI for the Dippy app
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
